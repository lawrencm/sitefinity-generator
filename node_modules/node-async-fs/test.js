const fs = require('./index.js')
async function test () {
    const chalk = await fs.mkdir('./test')
    const info = await fs.stat('.')
    const data = await fs.readFile('./index.js')
    console.log(chalk)
    console.log(info)
    console.log(data.toString('utf8'))
}
test()